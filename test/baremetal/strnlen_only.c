/* Kernel's strnlen - exactly as in lib/string.c */
typedef unsigned long size_t;

size_t strnlen(const char *s, size_t count)
{
	const char *sc;

	for (sc = s; count-- && *sc != '\0'; ++sc)
		/* nothing */;
	return sc - s;
}
