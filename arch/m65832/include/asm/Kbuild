# SPDX-License-Identifier: GPL-2.0
#
# M65832 asm header Kbuild
#
# Only list headers here that have an asm-generic version but are NOT
# in the mandatory-y list in include/asm-generic/Kbuild (those are
# auto-generated automatically).

generic-y += extable.h
generic-y += kvm_para.h
generic-y += mcs_spinlock.h
generic-y += param.h
generic-y += parport.h
generic-y += qrwlock.h
generic-y += qspinlock.h
generic-y += syscalls.h
generic-y += tlb.h
generic-y += user.h
generic-y += vmlinux.lds.h
